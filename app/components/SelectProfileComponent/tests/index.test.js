// import React from 'react';
// import { shallow } from 'enzyme';

// import SelectProfileComponent from '../index';

describe('<SelectProfileComponent />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
