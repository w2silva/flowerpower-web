// import React from 'react';
// import { shallow } from 'enzyme';

// import AppointmentComponent from '../index';

describe('<AppointmentComponent />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
